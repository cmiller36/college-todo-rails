
  <%= form_for [@story, @comment], {role: "form"} do |f| %>
 
          <div class="field">
            <%= f.label "Add a Comment:" %>
            <%= f.text_field :content, class: "form-control commentText", placeholder: 'Add a comment...', id: "comment_content_#{@story.id}" %>
          </div>
          <%= f.hidden_field :user_id, value: current_user.id %>
          <%= f.hidden_field :story_id, value: @story.id %>
          <div class="actions">
            <%= f.button "Submit", class: "btn btn-sm btn-primary"  %>
          </div>
  <% end %>
